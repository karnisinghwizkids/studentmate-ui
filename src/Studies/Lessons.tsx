// import { getFundaContent } from '../api/client';

// In your component
/*)
useEffect(() => {
  const loadContent = async () => {
    const { data } = await getFundaContent();
    // Handle the data
  };
  loadContent();
}, []);
*/
